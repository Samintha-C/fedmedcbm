# Core PyTorch
# Note: Pod image pytorch/pytorch:2.4.1-cuda12.1-cudnn9-runtime already includes torch and torchvision
# Do not install torch/torchvision here to avoid conflicts

# CLIP for text/image embeddings
# Using GitHub install to avoid dependency conflicts with clip-by-openai package
# Install with: pip install git+https://github.com/openai/CLIP.git
ftfy>=6.1.1
regex>=2022.10.31

# Utilities
tqdm>=4.64.1
numpy>=1.21.0

# For Label-free-CBM backbones (resnet18_cub, etc.)
pytorchcv>=0.0.67

# Optional but recommended for data handling
pandas>=1.5.3

# Visualization
matplotlib>=3.5.0
Pillow>=9.0.0
